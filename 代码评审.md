---
name: git-diff-reviewer
description: 查看当前项目 git diff 并自动进行代码评审。支持查看暂存区变更、工作区变更、分支间差异，并基于代码规范、最佳实践、潜在问题等维度进行智能评审。当用户提到"代码评审"、"code review"、"查看变更"、"git diff"、"检查代码"、"评审代码"、"review代码"、"代码审查"时使用此 skill。
---

# Git Diff 代码评审 Skill

## 概述

本 Skill 提供当前项目的 git diff 查看和自动化代码评审功能。通过分析代码变更，从代码规范、最佳实践、潜在问题、性能优化等多个维度进行智能评审，帮助开发者提升代码质量。

**核心特点**：
- 🔍 **多模式 Diff 查看**：支持工作区、暂存区、分支间、提交间的差异查看
- 🤖 **智能代码评审**：自动识别代码问题并提供改进建议
- 📊 **多维度分析**：代码规范、安全性、性能、可维护性等
- 🎯 **项目级执行**：仅在当前项目目录下运行，避免误操作
- 💡 **可操作建议**：提供具体的代码修改建议和最佳实践

## 一、Diff 查看模式

### 1.1 支持的 Diff 模式

```javascript
const diffModes = {
  // 工作区变更（未暂存的修改）
  workingChanges: {
    command: 'git diff',
    description: '查看工作区未暂存的变更',
    useCase: '查看当前修改但未 add 的内容'
  },
  
  // 暂存区变更（已暂存未提交）
  stagedChanges: {
    command: 'git diff --cached',
    description: '查看暂存区已 add 但未 commit 的变更',
    useCase: '查看即将提交的内容'
  },
  
  // 所有变更（工作区 + 暂存区）
  allChanges: {
    command: 'git diff HEAD',
    description: '查看所有未提交的变更',
    useCase: '查看相对于最近一次提交的所有改动'
  },
  
  // 分支间差异
  branchDiff: {
    command: 'git diff <source-branch> <target-branch>',
    description: '查看两个分支之间的差异',
    useCase: '比较分支差异，常用于 PR 前检查'
  },
  
  // 提交间差异
  commitDiff: {
    command: 'git diff <commit1> <commit2>',
    description: '查看两个提交之间的差异',
    useCase: '比较特定提交之间的变化'
  },
  
  // 最近 N 次提交的变更
  recentCommits: {
    command: 'git diff HEAD~N HEAD',
    description: '查看最近 N 次提交的变更',
    useCase: '查看最近几次提交的累积改动'
  }
};
```

### 1.2 Diff 查看执行流程

**步骤 1：确认当前工作目录**

首先确认执行目录是否为 git 仓库：

```bash
# 检查是否在 git 仓库中
git rev-parse --is-inside-work-tree

# 获取仓库根目录
git rev-parse --show-toplevel

# 显示当前分支
git branch --show-current
```

**步骤 2：询问用户选择 Diff 模式**

根据用户需求选择合适的 diff 模式，默认使用 `git diff HEAD` 查看所有未提交的变更。

**步骤 3：执行 Diff 命令**

```bash
# 根据选择的模式执行相应命令
# 示例：查看所有未提交的变更
git diff HEAD

# 如果需要查看统计信息
git diff HEAD --stat

# 如果需要查看具体文件的变更
git diff HEAD -- <file-path>
```

## 二、代码评审流程

### 2.1 评审执行步骤

**步骤 1：获取 Diff 内容**

```bash
# 获取完整的 diff 内容，包含上下文
git diff HEAD --unified=5
```

**步骤 2：分析变更文件类型**

识别变更的文件类型，针对性地进行评审：

```javascript
const fileTypeAnalysis = {
  javascript: {
    extensions: ['.js', '.jsx', '.mjs', '.cjs'],
    focus: ['ES6+ 语法', '异步处理', '模块化', '错误处理']
  },
  
  typescript: {
    extensions: ['.ts', '.tsx'],
    focus: ['类型定义', '接口设计', '泛型使用', '类型安全']
  },
  
  vue: {
    extensions: ['.vue'],
    focus: ['组件结构', '生命周期', 'Props/Events', '响应式数据']
  },
  
  css: {
    extensions: ['.css', '.scss', '.sass', '.less'],
    focus: ['选择器命名', '样式隔离', '响应式设计', '性能优化']
  },
  
  config: {
    extensions: ['.json', '.yaml', '.yml', '.config.js'],
    focus: ['配置格式', '环境变量', '敏感信息', '依赖版本']
  }
};
```

**步骤 3：执行代码评审**

从以下维度进行评审：

### 2.2 评审维度

```javascript
const reviewDimensions = {
  // 1. 代码规范
  codeStyle: {
    checks: [
      // 命名规范
      '变量命名：是否使用 camelCase（小驼峰）',
      '常量命名：是否使用 UPPER_SNAKE_CASE（全大写下划线分隔）',
      '函数命名：是否使用 camelCase，并以动词开头',
      '布尔值函数命名：是否以 is/has/can/should 开头',
      '类命名：是否使用 PascalCase（大驼峰）',
      '接口命名：是否使用 PascalCase，建议以 I 开头',
      '类型别名命名：是否使用 PascalCase',
      '枚举命名：是否使用 PascalCase',
      '文件命名：组件文件是否为 PascalCase，工具函数文件是否为 camelCase',
      // 代码格式
      '代码格式：缩进、空格、换行是否统一',
      '导入顺序：是否按标准库、第三方库、内部模块、类型导入的顺序',
      // 注释规范
      '注释质量：是否有必要的注释，注释是否清晰',
      'JSDoc 注释：函数是否有完整的参数和返回值说明',
      'TODO/FIXME：是否有未处理的标记',
      // 代码长度
      '代码长度：单个函数是否过长（建议不超过 50 行）',
      '文件长度：单个文件是否过长（建议不超过 300 行）',
      '代码重复：是否存在重复代码，是否需要抽取',
      // 变量声明
      '变量声明：是否优先使用 const，其次是 let，避免 var',
      '变量初始化：声明时是否初始化',
      '链式声明：是否避免链式声明（let a = b = c = 1）'
    ],
    severity: 'warning'
  },
  
  // 2. 最佳实践
  bestPractices: {
    checks: [
      // 设计原则
      '单一职责：函数/组件是否职责单一',
      '代码复用：是否合理复用现有代码',
      // 数据类型与类型安全
      '类型定义：是否使用正确的 TypeScript 类型',
      '可选参数：可选参数是否正确标注（使用 ?）',
      '联合类型：是否正确使用联合类型',
      '类型守卫：是否使用类型守卫而非类型断言',
      '泛型使用：是否合理使用泛型约束',
      // 现代语法
      'const 断言：是否合理使用 as const',
      '可选链：是否使用可选链（?.）替代嵌套判断',
      '空值合并：是否使用 ?? 替代 || 处理 null/undefined',
      '解构赋值：是否使用解构赋值简化代码',
      '模板字符串：是否使用模板字符串拼接字符串',
      '对象展开：是否使用 ... 替代 Object.assign',
      // 错误处理
      '错误处理：是否正确处理异常情况',
      '边界条件：是否考虑边界情况和空值',
      '防御性编程：输入参数是否需要校验',
      '自定义错误：是否使用自定义错误类型',
      // 其他
      '魔法数字：是否使用枚举或常量替代魔法数字',
      '循环优化：是否优先使用 for...of 而非 for 循环',
      '数据结构：是否使用 Set/Map 替代对象进行查找'
    ],
    severity: 'warning'
  },
  
  // 3. 潜在问题
  potentialIssues: {
    checks: [
      // 类型问题
      '类型错误：类型定义是否正确，是否可能存在类型错误',
      'any 类型：是否滥用 any 类型',
      '类型断言：是否过度使用类型断言',
      // 空值处理
      '空指针：是否可能访问 null/undefined',
      '数组越界：是否可能访问数组越界元素',
      // 内存问题
      '内存泄漏：是否存在内存泄漏风险（未清理的定时器、事件监听等）',
      '资源释放：是否正确释放资源（文件句柄、数据库连接等）',
      // 异步问题
      '并发问题：是否存在竞态条件',
      '异步处理：Promise/async-await 是否正确使用',
      '错误处理：异步函数是否有 try-catch',
      'Promise 链：Promise 链是否正确返回',
      // 其他问题
      '死循环：是否存在无限循环风险',
      '递归溢出：递归函数是否有正确的终止条件'
    ],
    severity: 'error'
  },
  
  // 4. 性能优化
  performance: {
    checks: [
      // 算法优化
      '算法复杂度：是否存在性能较差的算法',
      '循环优化：循环中是否有不必要的操作',
      '查找优化：是否使用合适的数据结构（Set/Map）',
      // 函数优化
      '防抖节流：频繁触发的函数是否需要防抖或节流',
      '记忆化：计算密集型函数是否需要缓存结果',
      '惰性初始化：是否可以使用惰性初始化延迟计算',
      // 渲染优化
      '渲染性能：组件渲染是否可以优化',
      '虚拟列表：长列表是否使用虚拟滚动',
      // 网络优化
      '网络请求：是否有重复请求，是否可以缓存',
      '并发控制：是否合理控制并发请求数量',
      '请求超时：是否设置合理的超时时间',
      // 资源优化
      '资源加载：大文件是否懒加载',
      '代码分割：是否进行代码分割',
      'tree-shaking：是否有未使用的导入'
    ],
    severity: 'suggestion'
  },
  
  // 5. 安全问题
  security: {
    checks: [
      'XSS 风险：是否存在未转义的用户输入',
      '敏感信息：是否暴露密码、token 等敏感信息',
      '权限控制：是否有正确的权限校验',
      '数据验证：是否验证用户输入',
      '第三方依赖：是否引入有漏洞的依赖'
    ],
    severity: 'error'
  },
  
  // 6. 可维护性
  maintainability: {
    checks: [
      // 代码组织
      '代码可读性：逻辑是否清晰易懂',
      '模块化：是否合理划分模块',
      '文件结构：文件头部是否有文件说明',
      '导入组织：导入语句是否按规范顺序排列',
      '导出规范：导出是否清晰，是否使用命名导出和默认导出',
      // 类与对象
      '类设计：类是否职责单一，是否有合适的访问修饰符',
      '接口设计：接口是否合理，是否需要拆分或合并',
      '继承关系：继承是否合理，是否过度继承',
      // 测试与文档
      '测试覆盖：关键逻辑是否有测试',
      '文档完整性：API 是否有文档',
      '注释质量：复杂逻辑是否有必要的注释',
      // 依赖管理
      '依赖关系：依赖是否清晰，是否存在循环依赖',
      '模块耦合：模块间耦合度是否合理'
    ],
    severity: 'info'
  }
};
```

## 三、评审报告生成

### 3.1 报告格式

生成结构化的评审报告：

```markdown
# 代码评审报告

## 📊 变更概览

- **分支**: [当前分支名]
- **变更文件数**: [数量]
- **新增行数**: [+lines]
- **删除行数**: [-lines]
- **评审时间**: [时间戳]

## 📁 变更文件列表

| 文件路径 | 状态 | 变更行数 |
|---------|------|---------|
| path/to/file.js | Modified | +10/-5 |
| ... | ... | ... |

## 🔍 评审结果

### ❌ 严重问题 (Errors)

#### 文件: `src/utils/helper.js`

**问题 1: 潜在的空指针访问**
- **位置**: Line 45
- **代码**: `const value = data.items[0].name`
- **问题描述**: `data.items` 可能为空数组，访问 `[0]` 会导致错误
- **建议修改**:
```javascript
const value = data.items?.[0]?.name || 'default'
```

---

### ⚠️ 警告 (Warnings)

#### 文件: `src/components/Button.vue`

**问题 1: 组件命名不规范**
- **位置**: Line 12
- **代码**: `const btn_click = () => {}`
- **问题描述**: 函数名应使用 camelCase 命名规范
- **建议修改**:
```javascript
const btnClick = () => {}
```

---

### 💡 优化建议 (Suggestions)

#### 文件: `src/api/user.js`

**建议 1: 添加请求缓存**
- **位置**: Line 23-30
- **问题描述**: 频繁调用 getUserInfo 接口可能导致重复请求
- **建议**: 考虑添加缓存机制或请求合并

---

## 📈 评审统计

| 类型 | 数量 |
|-----|------|
| 严重问题 | 2 |
| 警告 | 5 |
| 优化建议 | 3 |
| 信息提示 | 1 |

## ✅ 总体评价

代码整体质量 [良好/一般/需改进]，建议在提交前修复标记的严重问题。

## 📝 后续建议

1. [具体建议 1]
2. [具体建议 2]
3. [具体建议 3]
```

## 四、执行流程

### 4.1 完整执行步骤

```
用户请求代码评审
    │
    ├─ 1. 环境检查
    │   ├─ 检查是否在 git 仓库中
    │   ├─ 获取当前分支信息
    │   └─ 确认工作目录
    │
    ├─ 2. 询问 Diff 模式
    │   ├─ 工作区变更 (git diff)
    │   ├─ 暂存区变更 (git diff --cached)
    │   ├─ 所有变更 (git diff HEAD)
    │   ├─ 分支间差异 (需要指定分支)
    │   └─ 最近提交变更 (需要指定数量)
    │
    ├─ 3. 获取 Diff 内容
    │   ├─ 执行 git diff 命令
    │   ├─ 获取变更文件列表
    │   └─ 统计变更行数
    │
    ├─ 4. 代码分析
    │   ├─ 按文件类型分类
    │   ├─ 解析变更内容
    │   └─ 提取代码上下文
    │
    ├─ 5. 执行评审
    │   ├─ 代码规范检查
    │   ├─ 最佳实践检查
    │   ├─ 潜在问题检查
    │   ├─ 性能优化检查
    │   ├─ 安全问题检查
    │   └─ 可维护性检查
    │
    ├─ 6. 生成报告
    │   ├─ 汇总评审结果
    │   ├─ 按严重程度分类
    │   ├─ 生成改进建议
    │   └─ 输出评审报告
    │
    └─ 7. 提供后续操作建议
        ├─ 是否需要修复问题
        ├─ 是否需要查看详细说明
        └─ 是否需要生成 commit message
```

### 4.2 交互式选项

在评审完成后，提供以下交互选项：

```javascript
const postReviewActions = [
  {
    key: 'fix',
    label: '帮我修复问题',
    action: '根据评审结果自动修复可修复的问题'
  },
  {
    key: 'explain',
    label: '详细解释问题',
    action: '对某个问题进行详细解释和示例说明'
  },
  {
    key: 'commit-msg',
    label: '生成 commit message',
    action: '根据变更内容生成规范的 commit message'
  },
  {
    key: 'export',
    label: '导出评审报告',
    action: '将评审报告导出为 Markdown 文件'
  }
];
```

## 五、使用示例

### 示例 1：查看当前工作区变更并评审

**用户输入**：
```
帮我评审当前项目的代码变更
```

**执行流程**：
1. 检查当前在 git 仓库中
2. 默认执行 `git diff HEAD` 查看所有未提交变更
3. 分析变更内容
4. 执行多维度评审
5. 输出评审报告

### 示例 2：查看分支间差异并评审

**用户输入**：
```
对比 feature/user-module 和 develop 分支的差异并评审
```

**执行流程**：
1. 执行 `git diff feature/user-module develop`
2. 分析分支间差异
3. 执行代码评审
4. 输出评审报告

### 示例 3：查看最近提交并评审

**用户输入**：
```
评审最近 3 次提交的代码
```

**执行流程**：
1. 执行 `git diff HEAD~3 HEAD`
2. 分析最近 3 次提交的变更
3. 执行代码评审
4. 输出评审报告

## 六、注意事项

### 6.1 执行限制

- **仅在当前项目目录执行**：不会跨目录或跨仓库执行
- **只读操作**：默认只查看 diff，不自动修改代码
- **敏感信息保护**：评审时会检测并提醒敏感信息泄露

### 6.2 最佳实践建议

1. **提交前评审**：建议在每次 commit 前执行代码评审
2. **分支对比**：PR 前使用分支间差异评审
3. **持续改进**：根据评审结果持续优化代码质量
4. **团队规范**：可根据团队规范自定义评审规则

## 七、详细规范参考

### 7.1 命名规范

```javascript
// ✅ 正确示范
const userName = '张三';                          // 变量：camelCase
const MAX_RETRY_COUNT = 3;                       // 常量：UPPER_SNAKE_CASE
function getUserInfo(userId) {}                  // 函数：camelCase + 动词开头
function isValidEmail(email) {}                  // 布尔函数：is/has/can/should 开头
class UserService {}                             // 类：PascalCase
interface IUser {}                               // 接口：PascalCase + I 开头
type UserRole = 'admin' | 'user';               // 类型别名：PascalCase
enum UserStatus { Active = 'ACTIVE' }           // 枚举：PascalCase

// ❌ 错误示范
const user_name = '张三';                        // 应使用 camelCase
const maxRetryCount = 3;                         // 常量应使用 UPPER_SNAKE_CASE
function user() {}                               // 函数应以动词开头
class userService {}                              // 类应使用 PascalCase
```

### 7.2 变量声明规范

```javascript
// ✅ 正确示范
const PI = 3.14159;                              // 优先使用 const
let counter = 0;                                 // 需要修改时使用 let
let name = '';                                   // 声明时初始化

// ❌ 错误示范
var age = 18;                                    // 避免使用 var
let a = b = c = 1;                              // 避免链式声明
```

### 7.3 类型规范

```javascript
// ✅ 正确示范
const numbers: number[] = [1, 2, 3];
const names: Array<string> = ['Alice', 'Bob'];
let id: string | number;                         // 联合类型
type Direction = 'up' | 'down' | 'left' | 'right'; // 字面量类型

// 使用类型守卫
function isString(value: unknown): value is string {
  return typeof value === 'string';
}

// ❌ 错误示范
let data: any = {};                              // 避免使用 any
const value = data as string;                    // 避免过度使用类型断言
```

### 7.4 函数规范

```javascript
// ✅ 正确示范
// 箭头函数（推荐）
const multiply = (a: number, b: number): number => a * b;

// 参数默认值
function greet(name: string, greeting: string = '你好'): string {
  return `${greeting}，${name}！`;
}

// 可选参数
function buildName(firstName: string, lastName?: string): string {
  return lastName ? `${firstName} ${lastName}` : firstName;
}

// 异步函数
async function fetchUser(id: number): Promise<IUser> {
  try {
    const response = await fetch(`/api/users/${id}`);
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    return response.json();
  } catch (error) {
    console.error('获取用户失败:', error);
    throw error;
  }
}

// ❌ 错误示范
function fetchUser(id) {                          // 缺少类型定义
  return fetch('/api/users/' + id)               // 应使用模板字符串
    .then(res => res.json());                    // 应使用 async/await
}
```

### 7.5 类与对象规范

```javascript
// ✅ 正确示范
class User {
  private id: number;
  public email: string;
  readonly createdAt: Date;
  static readonly ROLE_USER = 'user';

  constructor(id: number, public name: string) {
    this.id = id;
    this.createdAt = new Date();
  }

  getDisplayName(): string {
    return `${this.name} <${this.email}>`;
  }
}

// 简写形式
class Product {
  constructor(
    public id: number,
    public name: string,
    private price: number
  ) {}
}

// ❌ 错误示范
class user {                                      // 类名应使用 PascalCase
  constructor(id, name) {                        // 缺少类型定义
    this.id = id;
  }
}
```

### 7.6 异步编程规范

```javascript
// ✅ 正确示范
// async/await（推荐）
async function fetchUserData(userId: number): Promise<IUser> {
  try {
    const response = await fetch(`/api/users/${userId}`);
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    const user = await response.json();
    return user;
  } catch (error) {
    console.error('获取用户数据失败:', error);
    throw error;
  } finally {
    console.log('请求完成');
  }
}

// 并行执行
async function fetchAllData(): Promise<[IUser[], IProduct[]]> {
  const [users, products] = await Promise.all([
    fetch('/api/users'),
    fetch('/api/products')
  ]);
  return Promise.all([users.json(), products.json()]);
}

// ❌ 错误示范
async function fetchData() {
  const response = await fetch('/api/data');     // 缺少错误处理
  return response.json();
}
```

### 7.7 错误处理规范

```javascript
// ✅ 正确示范
// try-catch
try {
  JSON.parse('invalid json');
} catch (error) {
  if (error instanceof SyntaxError) {
    console.error('JSON 解析错误:', error.message);
  }
}

// 自定义错误
class ValidationError extends Error {
  constructor(public field: string, message: string) {
    super(message);
    this.name = 'ValidationError';
  }
}

// 可选链和空值合并
const userName = user?.profile?.name ?? '未知用户';

// ❌ 错误示范
const data = JSON.parse(jsonStr);                 // 缺少错误处理
```

### 7.8 注释规范

```javascript
// ✅ 正确示范
/**
 * 计算两个数的和
 * @param a - 第一个数
 * @param b - 第二个数
 * @returns 两数之和
 * @example
 * ```ts
 * const result = add(1, 2); // 3
 * ```
 */
function addNumbers(a: number, b: number): number {
  return a + b;
}

// 单行注释 - 说明下一行代码的目的
const timeout = 5000; // 超时时间（毫秒）

/*
 * 多行注释
 * 用于解释复杂的逻辑
 */

// TODO: 待实现的功能
// FIXME: 需要修复的问题
// HACK: 临时解决方案
// NOTE: 重要说明
```

### 7.9 代码组织规范

```javascript
// ✅ 正确示范 - 文件结构
/**
 * @fileoverview 用户服务模块
 * @author 张三
 * @version 1.0.0
 * @date 2024-01-01
 */

// 1. 标准库
import fs from 'fs';
import path from 'path';

// 2. 第三方库
import express from 'express';
import lodash from 'lodash';

// 3. 项目内部模块
import { UserService } from './services';
import { formatDate } from './utils';

// 4. 类型导入
import type { IUser } from './types';

// 5. 常量
const MAX_ITEMS = 100;

// 6. 接口/类型定义
interface ILocalConfig {}

// 7. 类
class LocalService {}

// 8. 函数
function helper() {}

// 9. 导出
export { LocalService, helper };
```

### 7.10 性能优化规范

```javascript
// ✅ 正确示范
// 防抖
function debounce(func, wait) {
  let timeoutId = null;
  return function(...args) {
    if (timeoutId) clearTimeout(timeoutId);
    timeoutId = setTimeout(() => func.apply(this, args), wait);
  };
}

// 节流
function throttle(func, limit) {
  let inThrottle = false;
  return function(...args) {
    if (!inThrottle) {
      func.apply(this, args);
      inThrottle = true;
      setTimeout(() => inThrottle = false, limit);
    }
  };
}

// 记忆化
function memoize(fn) {
  const cache = new Map();
  return (...args) => {
    const key = JSON.stringify(args);
    if (cache.has(key)) return cache.get(key);
    const result = fn(...args);
    cache.set(key, result);
    return result;
  };
}

// 使用 Set/Map 进行查找
const uniqueItems = new Set(items);
const userMap = new Map<string, IUser>();

// for...of 替代 for 循环
for (const item of items) {
  console.log(item);
}
```

## 八、评审规则扩展

### 7.1 自定义规则

支持根据项目特点自定义评审规则：

```javascript
const customRules = {
  // 项目特定规则
  projectRules: [
    {
      id: 'no-console',
      pattern: /console\.(log|warn|error)/,
      message: '生产代码中不应包含 console 语句',
      severity: 'warning'
    },
    {
      id: 'no-debugger',
      pattern: /debugger/,
      message: '生产代码中不应包含 debugger 语句',
      severity: 'error'
    }
  ],
  
  // 团队规范
  teamStandards: {
    maxLineLength: 120,
    maxFunctionLength: 50,
    maxFileLength: 300,
    namingConvention: {
      variable: 'camelCase',
      constant: 'UPPER_SNAKE_CASE',
      component: 'PascalCase',
      file: 'kebab-case'
    }
  }
};
```

### 7.2 规则优先级

```
严重问题 (Error) > 警告 (Warning) > 建议 (Suggestion) > 信息 (Info)
```

## 八、总结

本 Skill 提供完整的 git diff 查看和代码评审功能：

1. **灵活的 Diff 模式**：支持多种场景的差异查看
2. **全面的评审维度**：从 6 个维度进行代码审查
3. **可操作的报告**：提供具体的问题位置和修改建议
4. **项目级安全**：仅在当前项目目录执行，避免误操作
5. **持续优化**：支持自定义规则，适应团队规范

使用本 Skill 可以有效提升代码质量，减少潜在问题，是代码提交前的得力助手。
